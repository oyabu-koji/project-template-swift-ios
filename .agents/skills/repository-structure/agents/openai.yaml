interface:
  display_name: "Repository Structure"
  short_description: "アーキテクチャを具体的な配置・命名・依存規則へ変換する"
  default_prompt: "Use $repository-structure to create or update the repository structure document."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "Steering"
  short_description: "task単位の要求・設計・進捗・検証証跡を維持する"
  default_prompt: "Use $steering to maintain the selected task plan and progress record."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "Prepare Steering"
  short_description: "確定した追加仕様から実装用steeringの要求・設計・tasklistを準備する"
  default_prompt: "Use $prepare-steering to prepare requirements, design, and tasklist from the selected confirmed feature specification."
policy:
  allow_implicit_invocation: false

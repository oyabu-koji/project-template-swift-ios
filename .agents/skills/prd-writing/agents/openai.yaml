interface:
  display_name: "PRD Writing"
  short_description: "初期要件から検証可能なプロダクト要求定義書を作成する"
  default_prompt: "Use $prd-writing to create or update the product requirements document."
policy:
  allow_implicit_invocation: false

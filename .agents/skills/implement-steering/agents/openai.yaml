interface:
  display_name: "Implement Steering"
  short_description: "指定steeringに従って実装・テスト・進捗・検証証跡を同期する"
  default_prompt: "Use $implement-steering to implement the selected steering requirements, design, and tasklist and record progress and verification evidence."
policy:
  allow_implicit_invocation: false

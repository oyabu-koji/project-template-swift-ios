interface:
  display_name: "Setup Project"
  short_description: "初期要件から6つの永続プロジェクト文書を依存順に作成する"
  default_prompt: "Use $setup-project to create the six durable project documents from valid initial requirements."
policy:
  allow_implicit_invocation: false
